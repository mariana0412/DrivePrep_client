import '../App.css';
import AppNavbar from './AppNavbar/AppNavbar';
import ModeSelection from "./ModeSelection/ModeSelection";

const Home = () => {

    return (
        <div>
            <AppNavbar/>
            <ModeSelection/>
        </div>
    );
};

export default Home;